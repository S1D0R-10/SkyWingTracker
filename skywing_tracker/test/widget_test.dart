// Basic smoke test placeholder — full tests to be added later.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test placeholder', (tester) async {
    // Full integration tests require Firebase + Supabase mocking.
    expect(true, isTrue);
  });
}
